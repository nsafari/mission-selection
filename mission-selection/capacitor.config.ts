import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.missionselection.app',
  appName: 'Mission Selection',
  webDir: 'dist/mission-selection/browser'
};

export default config;
